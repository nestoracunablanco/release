workflow:
  as: kubevirt-libvirt-vpn
  steps:
    allow_best_effort_post_steps: true
    pre:
    - ref: upi-libvirt-vpn-kubevirt-pre
    dnsConfig:
      nameservers:
      - 192.168.1.1
      - 9.9.9.9
  documentation: |-
    This workflow is for the multiach and IBM-Z teams to test connectivity to the new Z CI
    environment.
